{
  "name": "hexward-blog",
  "version": "1.0.0",
  "description": "Hexward Consulting blog",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "test": "npm run clean && npm run build && node --test test/site.test.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-rss": "^1.2.0"
  }
}
